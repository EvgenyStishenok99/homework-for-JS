// Напишите функцию calculateAverage, которая принимает 2 числа и возвращает их среднее арифметическое
const num1 = 40
const num2 = 45

function calculateAverage(first_number, second_number) {
 const sum = first_number + second_number;
 const average = sum/2
return average;
 }


const fullNum = calculateAverage(num1,num2)

console.log(fullNum)

